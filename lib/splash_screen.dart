import 'package:flutter/material.dart';

class SpalishScreen extends StatelessWidget {
  const SpalishScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(" spalishscreen"),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
